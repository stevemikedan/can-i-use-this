# Runtime AI — ONLY Google Cloud AI services and Parallel are permitted
# (docs/COMPLIANCE.md §3). Never add openai, anthropic, AWS, or Azure here.
parallel-web>=0.1          # Tier 3. Search API on the primary request path (COMPLIANCE §1)
google-adk>=1.0            # accepted Google Cloud SDK (COMPLIANCE §2)

httpx>=0.27
pydantic>=2.5
pyyaml>=6
pytest>=8

# api/ — FastAPI with SSE on Cloud Run
fastapi>=0.115
uvicorn[standard]>=0.30

# Cloud Run cache backend (CACHE_BACKEND=firestore); sqlite locally
google-cloud-firestore>=2.16
